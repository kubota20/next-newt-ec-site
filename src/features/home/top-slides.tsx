import { TopImages } from "@/features/home/top-images";

const TopSlides = () => {
  return (
    <>
      <section className="bg-black">
        <TopImages />
      </section>
    </>
  );
};

export default TopSlides;
